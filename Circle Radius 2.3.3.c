#include <stdio.h>

#define PI 3.14159

int
main (void)
{
	double Radius, Area
	printf("Please enter the radius of the circle > \n");
	scanf("%lf", Radius);
	Area = PI * Radius * Radius
	printf("The circle's area is %f", Area);

	return (0);

}